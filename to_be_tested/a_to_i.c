#include <stdio.h>

/* a_to_i - A atoi function assci to integer.
 * @c: The char to be changed to an integer.
 *
 * Return: The converted interger.
 */

int a_to_i(char c)
{
	/*tought we might need this for creating the adresses*/
	char c = 'a';
	int i = (int)c;
	return (c);
}
